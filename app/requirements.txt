# Fuente única de dependencias de la aplicación web.
#
# Este archivo se conserva porque varios flujos históricos ejecutan:
#   cd app && pip install -r requirements.txt
# La lista canónica vive en la raíz del repositorio para que desarrollo,
# herramientas de VPS y producción instalen exactamente las mismas versiones.
-r ../requirements.txt
